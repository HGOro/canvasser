<script src="https://cdnjs.cloudflare.com/ajax/libs/Chart.js/2.8.0/Chart.min.js"></script>

<h1>Welcome to Canvasser</h1>
{{!-- home page we can display a bootstrap nav bar and a jumbotron would be cool to use some graph api to plot all the survey data in  a visual way--}}
<canvas id="myChart" width="100" height="50"></canvas>
<canvas id="myChart2" width="100" height="50"></canvas>
<canvas id="myChart3" width="100" height="50"></canvas>
<canvas id="myChart4" width="100" height="50"></canvas>

<script>
    //party graph=============================== 
    $.get("/api/survey/all", function(response){
            var democrat = 0;
            var republican = 0;
            var independent = 0;
            var undecided = 0;
            var abstain = 0;
            var partyCount = [];
            response.forEach(function(user, index){
                console.log(user)
                console.log(user.party)
                if(user.party === "Democrat"){
                    democrat++
                }
                if(user.party === "Republican"){
                    republican++
                }
                if(user.party === "Independent"){
                independent++
                }
                if(user.party === "Undecided"){
                undecided++
                }
                if(user.party === "Abstain"){
                abstain++
                }
            })
            partyCount.push(democrat, republican, independent, undecided, abstain)
            createPartyGraph(partyCount)
        })
        
    function createPartyGraph(partyCount){
        var ctx = document.getElementById('myChart');
        var myChart = new Chart(ctx, {
            type: 'doughnut',
            data: {
                labels: ['Democrat','Republican','Independent','Undecided','Abstain'],
                datasets: [{
                    data: partyCount,
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)',
                        'rgba(75, 192, 192, 0.2)',
                        'rgba(153, 102, 255, 0.2)',
                        'rgba(255, 159, 64, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)',
                        'rgba(75, 192, 192, 1)',
                        'rgba(153, 102, 255, 1)',
                        'rgba(255, 159, 64, 1)'
                    ],
                    borderWidth: 1
                }] 
            },
            options: {
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        });
    }
    //nominee graph ===================================
    $.get("/api/survey/all", function(response){
        var bernieSanders = 0;
        var kamalaHarris = 0;
        var other = 0;
        var undecided = 0;
        var abstain = 0;
        var nomineeCount = [];
        response.forEach(function(user, index){
            console.log(user)
            console.log(user.demNominee)
            if(user.demNominee === "Bernie Sanders"){
                bernieSanders++
            }
            if(user.demNominee === "Kamala Harris"){
                kamalaHarris++
            }
            if(user.demNominee === "Other"){
            other++
            }
            if(user.demNominee === "Undecided"){
            undecided++
            }
            if(user.demNominee === "Abstain"){
            abstain++
            }
        })
        nomineeCount.push(bernieSanders, kamalaHarris, other, undecided, abstain)
        createDemNomineeGraph(nomineeCount)
    })

    function createDemNomineeGraph(nomineeCount){
        var ctx = document.getElementById('myChart2');
        var myChart = new Chart(ctx, {
            type: 'horizontalBar',
            data: {
                labels: ['Bernie Sanders','Kamala Harris','Other','Undecided','Abstain'],
                datasets: [{
                    data: nomineeCount,
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)',
                        'rgba(75, 192, 192, 0.2)',
                        'rgba(153, 102, 255, 0.2)',
                        'rgba(255, 159, 64, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)',
                        'rgba(75, 192, 192, 1)',
                        'rgba(153, 102, 255, 1)',
                        'rgba(255, 159, 64, 1)'
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        });
    }

    //proposition graph ===================================
    $.get("/api/survey/all", function(response){
        var yes = 0;
        var no = 0;
        var undecided = 0;
        var abstain = 0;
        var propCount = [];
        response.forEach(function(user, index){
            console.log(user)
            console.log(user.prop)
            if(user.prop === "Yes"){
                yes++
            }
            if(user.prop === "No"){
                no++
            }
            if(user.prop === "Undecided"){
            undecided++
            }
            if(user.prop === "Abstain"){
            abstain++
            }
        })
        propCount.push(yes, no, undecided, abstain)
        createPropGraph(propCount)
    })

    function createPropGraph(propCount){
        var ctx = document.getElementById('myChart3');
        var myChart = new Chart(ctx, {
            type: 'pie',
            data: {
                labels: ['Yes','No','Undecided','Abstain'],
                datasets: [{
                    data: propCount,
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)',
                        'rgba(75, 192, 192, 0.2)',
                        'rgba(153, 102, 255, 0.2)',
                        'rgba(255, 159, 64, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)',
                        'rgba(75, 192, 192, 1)',
                        'rgba(153, 102, 255, 1)',
                        'rgba(255, 159, 64, 1)'
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        });
    }
//4th graph, for now copied the nominee graph as a placeholder ===================================
    $.get("/api/survey/all", function(response){
        var bernieSanders = 0;
        var kamalaHarris = 0;
        var other = 0;
        var undecided = 0;
        var abstain = 0;
        var nomineeCount = [];
        response.forEach(function(user, index){
            console.log(user)
            console.log(user.demNominee)
            if(user.demNominee === "Bernie Sanders"){
                bernieSanders++
            }
            if(user.demNominee === "Kamala Harris"){
                kamalaHarris++
            }
            if(user.demNominee === "Other"){
            other++
            }
            if(user.demNominee === "Undecided"){
            undecided++
            }
            if(user.demNominee === "Abstain"){
            abstain++
            }
        })
        nomineeCount.push(bernieSanders, kamalaHarris, other, undecided, abstain)
        createPlaceHolderGraph(nomineeCount)
    })

    function createPlaceHolderGraph(nomineeCount){
        var ctx = document.getElementById('myChart4');
        var myChart = new Chart(ctx, {
            type: 'polarArea',
            data: {
                labels: ['Bernie Sanders','Kamala Harris','Other','Undecided','Abstain'],
                datasets: [{
                    data: nomineeCount,
                    backgroundColor: [
                        'rgba(255, 99, 132, 0.2)',
                        'rgba(54, 162, 235, 0.2)',
                        'rgba(255, 206, 86, 0.2)',
                        'rgba(75, 192, 192, 0.2)',
                        'rgba(153, 102, 255, 0.2)',
                        'rgba(255, 159, 64, 0.2)'
                    ],
                    borderColor: [
                        'rgba(255, 99, 132, 1)',
                        'rgba(54, 162, 235, 1)',
                        'rgba(255, 206, 86, 1)',
                        'rgba(75, 192, 192, 1)',
                        'rgba(153, 102, 255, 1)',
                        'rgba(255, 159, 64, 1)'
                    ],
                    borderWidth: 1
                }]
            },
            options: {
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        });
    }



</script>
